apiVersion: v1
kind: Service
metadata:
  name: mentoria-service
  namespace: mentoria
spec:
  type: NodePort
  selector:
    app: mentoria-app
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007